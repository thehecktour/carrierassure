# --- AI-ASSISTED ---
# Tool: Claude Sonnet 4.6
# Prompt: "Fix CI pipeline: upgrade Python 3.11 to 3.12 and replace
#          manage.py test with pytest for Django test runner."
# Modifications: Changed python-version to 3.12, replaced
#                'manage.py test' with 'pytest --tb=short -q'.
# --- END AI-ASSISTED ---

name: Carrier Assure CI

on:
  push:
    branches: ["main", "develop"]
  pull_request:
    branches: ["main", "develop"]

jobs:
  backend:
    name: Backend CI (Django)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Poetry
        run: |
          pip install poetry
          poetry config virtualenvs.create false

      - name: Install dependencies
        run: poetry install --no-interaction --no-root

      - name: Run Lint
        run: |
          poetry run ruff check .
          poetry run black --check .

      - name: Run Migrations Check
        run: poetry run python manage.py makemigrations --check --dry-run

      - name: Run Tests
        run: poetry run pytest --tb=short -q

  frontend:
    name: Frontend CI (React)
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run Lint
        run: npm run lint

      - name: Run Tests
        run: npm run test

      - name: Build Frontend
        run: npm run build